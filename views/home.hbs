<div class="container">
  {{#each houses}}
  <div class="house-list">
    <a href="/{{id}}"><img class="img-circle" src="{{image}}" alt="{{name}}"></a>
    <h2>{{name}}</h2>
  <div>Values: {{values}}</div>
  <br>
  </div>
  {{/each}}
</div>
